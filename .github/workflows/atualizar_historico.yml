name: Atualizar Histórico de Preços

on:
  schedule:
    - cron: '0 8 22 * *'  # Dia 22 às 8h UTC (5h Brasília) — teste; voltar para '0 8 1 * *' depois
  workflow_dispatch:        # Permite rodar manualmente

jobs:
  atualizar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Instalar dependências
        run: pip install selenium webdriver-manager

      - name: Rodar scraper de histórico
        run: python atualizar_historico.py

      - name: Salvar historico.json no repositório
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add app/historico.json
          git diff --staged --quiet || git commit -m "Histórico atualizado $(date +'%m/%Y')"
          git pull --rebase origin main
          git push
